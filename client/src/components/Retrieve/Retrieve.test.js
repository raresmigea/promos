import React from 'react';
import { render } from '@testing-library/react';

import Retrieve from './Retrieve';

describe('Retrieve', () => {
  test('renders Retrieve component', () => {
    render(<Retrieve />);
  });
});
